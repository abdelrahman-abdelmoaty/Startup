export type Testimonial = {
  img: string;
  quote: string;
  name: string;
  title: string;
};

export type Feature = {
  SVGElement: React.ReactNode;
  heading: string;
  paragraph: string;
};
